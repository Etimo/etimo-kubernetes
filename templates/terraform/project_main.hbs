# Buckets, if any
{{#each buckets}}
resource "digitalocean_spaces_bucket" "{{../project}}-{{name}}-{{../stage}}" {
  region = var.region
  name   = "{{../project}}-{{name}}-{{../stage}}"
}
{{/each}}


# Secret
apiVersion: v1
kind: Secret
metadata:
  name: provisioned-secrets
  namespace: "{{project}}"
  labels:
    provisioner: etimo-kubernetes
data:
  {{#each secrets}}
  {{key}}: "{{value}}"
  {{/each}}
